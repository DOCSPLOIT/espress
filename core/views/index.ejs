<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>API Documentation</title>
    <link href="/static/bootstrap.css" rel="stylesheet" />
    <script src="/static/popper.js"></script>
    <script src="/static/bootstrap.js"></script>
    <style>
      div.sidebar {
        height: 100vh;
        color: white;
      }

      a.active {
        background-color: var(--bs-primary);
      }

      a.active:hover {
        color: var(--bs-dark);
      }

      .accordion-button {
        background-color: var(--bs-dark) !important;
        box-shadow: none !important;
        outline: none;
        color: white !important;
      }

      button.accordion-button::after {
        background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 16 16' fill='%23fff'%3e%3cpath fill-rule='evenodd' d='M1.646 4.646a.5.5 0 0 1 .708 0L8 10.293l5.646-5.647a.5.5 0 0 1 .708.708l-6 6a.5.5 0 0 1-.708 0l-6-6a.5.5 0 0 1 0-.708z'/%3e%3c/svg%3e") !important;
      }

      button.accordion-button.text-white[data-method='post'] > p {
        background-color: var(--bs-blue);
      }
      button.accordion-button.text-white[data-method='get'] > p {
        background-color: #0dbc79;
      }
      button.accordion-button.text-white[data-method='delete'] > p {
        background-color: #a13d31;
      }
      button.accordion-button.text-white[data-method='put'] > p {
        background-color: #a472aa;
      }
    </style>
  </head>

  <body class="d-flex">
    <header><%- include('./partials/header'); %></header>

    <div class="d-flex vh-100 col-9 ms-auto">
      <div data-content-card="api" class="card col-md-12 p-3">
        <% if (data.name) { %> <%- include('./partials/accordion') %> <% } else { %>
        <p class="m-auto display-6">Module Not Found</p>
        <%}%>
      </div>
    </div>
  </body>
</html>
